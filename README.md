# NLP Text Generator

This project provides a Python-based tool for generating human-like text using advanced NLP techniques. The tool leverages state-of-the-art language models to create coherent and contextually relevant text.

## Features

- Generate text based on user-provided prompts.
- Supports multiple text generation models.
- Configurable parameters to fine-tune the generated text.
- Easy-to-use command-line interface.

## Requirements

- Python 3.7 or higher
- Required Python packages (listed in `requirements.txt`)

## Installation

1. **Clone the repository:**

    ```bash
    git clone https://github.com/aditya-kamatt/NLP-Text-Generator.git
    ```

2. **Navigate to the project directory:**

    ```bash
    cd NLP-Text-Generator
    ```

3. **Create a virtual environment (optional but recommended):**

    ```bash
    python -m venv venv
    ```

4. **Activate the virtual environment:**

    - On Windows:

        ```bash
        venv\Scripts\activate
        ```

    - On macOS/Linux:

        ```bash
        source venv/bin/activate
        ```

5. **Install the required packages:**

    ```bash
    pip install -r requirements.txt
    ```

## Usage

To generate text, use the provided command-line interface or run the `generate_text.py` script directly.

### Command-Line Interface

Run the script with the following command:

```bash
python generate_text.py --prompt "Your prompt here" --model "model_name" --length 100
```
- `--prompt`: The initial text to start the generation from.
- `--model`: The name of the pre-trained model to use (e.g., "gpt-2", "gpt-neo").
- `--length`: The length of the generated text (number of tokens).

## Example
```bash
python generate_text.py --prompt "Once upon a time" --model "gpt-2" --length 50
```
This command will generate 50 tokens of text starting with "Once upon a time" using the GPT-2 model.

## Configuration
Adjust the configuration settings in the `config.py` file to change default parameters such as model paths and text generation settings.

## What I learned
### Fundamentals of Natural Language Processing (NLP)
- **Text Preprocessing:**  
  I learned how to clean and prepare raw text data, which includes techniques such as tokenization, normalization, and handling special characters.
  
- **Understanding Language Models:**  
  I gained insights into how language models work and the challenges of generating coherent and contextually relevant text.

### Neural Network Architectures for Text Generation
- **Recurrent Neural Networks (RNNs):**  
  I explored the basics of RNNs and how they are used to process sequential data, which is crucial for text generation tasks.
  
- **LSTM and GRU Models:**  
  I delved into advanced RNN variants like LSTM and GRU that help address issues like the vanishing gradient problem, enabling the model to capture long-term dependencies.
  
- **Sampling Techniques:**  
  I learned about temperature sampling and other strategies to control the randomness and creativity of the text generated by the model.

### Practical Implementation Skills
- **Python Programming:**  
  Working on this project improved my Python skills as I wrote scripts, debugged code, and managed model training processes.
  
- **Deep Learning Libraries:**  
  I gained practical experience with deep learning frameworks such as TensorFlow, Keras, or PyTorch, which are instrumental in building and evaluating the model.
  
- **Hyperparameter Tuning:**  
  I experimented with various hyperparameters like learning rate, batch size, and epochs, and observed how these adjustments impact model performance.

### Data Handling and Experimentation
- **Data Collection and Cleaning:**  
  I learned to handle datasets, perform exploratory data analysis, and prepare the data for model training effectively.
  
- **Model Evaluation:**  
  I explored methods for evaluating the quality of the generated text, using both quantitative measures (like loss functions) and qualitative analysis.
  
- **Experimentation:**  
  The project encouraged me to experiment with different architectures and training strategies to see firsthand how various tweaks affect the output.

### Advanced Topics and Best Practices
- **Preventing Overfitting:**  
  I learned techniques such as dropout and regularization to mitigate overfitting and ensure the model generalizes well.
  
- **Optimizing Model Performance:**  
  I discovered strategies to improve the efficiency and performance of the model during training.
  
- **Real-World Applications:**  
  I gained an appreciation for how these techniques can be applied to practical tasks such as chatbots, creative writing, and automated content creation.

## Result
![1727519600956](https://github.com/user-attachments/assets/00dd45e1-4dc2-45a8-b68a-6dd7c3b78bbc)
